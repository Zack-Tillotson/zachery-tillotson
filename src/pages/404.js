import React from "react"

import Layout from "../components/Layout"

const NotFoundPage = () => (
  <Layout title="Not found">
    <h1>Oops</h1>
    <p>I couldn't find that page, check that the URL is correct?</p>
  </Layout>
)

export default NotFoundPage
